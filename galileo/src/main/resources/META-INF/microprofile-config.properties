# Microprofile server properties
server.port=8080
server.host=0.0.0.0

# Change the following to true to enable the optional MicroProfile Metrics REST.request metrics
metrics.rest-request.enabled=true

tracing.service=helidon-servicetracing.components.web-server.spans.0.name="HTTP Request"tracing.protocol=httptracing.host=localhosttracing.port=14250tracing.path=/api/traces/minetracing.token=tokentracing.propagation=jaeger
